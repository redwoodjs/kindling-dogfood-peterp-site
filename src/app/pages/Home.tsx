export function Home() {
  return (
    <main className="shell">
      <section className="card">
        <p className="eyebrow">RedwoodSDK migration scaffold</p>
        <h1>App shell is ready.</h1>
        <p>
          This workspace now has the RedwoodSDK runtime entrypoints and package setup
          needed for the migration. The existing static site content is intentionally
          untouched for now.
        </p>
      </section>
    </main>
  );
}
